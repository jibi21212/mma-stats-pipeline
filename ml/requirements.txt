# Python ML component dependencies (reads data/ufc.db produced by the Go scraper).
# Install with:  pip install -r requirements.txt
# Tested against Python 3.11.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.2          # model persistence for predict.py (also a scikit-learn dep)
matplotlib>=3.7
seaborn>=0.12
mlxtend>=0.22
jupyter>=1.0

# Optional: UMAP is import-guarded in archetypes.py and is NEVER a hard dependency.
# Uncomment to enable a UMAP 2-D projection alongside the PCA scatter.
# umap-learn>=0.5
